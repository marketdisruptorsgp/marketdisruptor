

# Rename Nav Item to "Access Modes"

## What Changes

A single text update in the dashboard's tab section header. The label above the analysis mode tabs will be changed from its current state to **"Access Modes"**.

## Technical Details

### File Modified

| File | Change |
|---|---|
| `src/pages/DashboardPage.tsx` | Update the tab section label/header text to "Access Modes" |

The TABS array labels remain as they are: Product, Service, Business Model (with Nostalgia hidden per previous conversation). Only the section heading above the tab bar changes to read "Access Modes."

